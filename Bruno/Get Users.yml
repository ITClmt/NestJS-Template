info:
  name: Get Users
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/users?take=1
  params:
    - name: take
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
